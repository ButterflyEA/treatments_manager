# Example environment configuration file
# Copy this file to .env and modify the values as needed

# Database Configuration
DATABASE_URL=sqlite:./patients.db?mode=rwc

# JWT Configuration (REQUIRED in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Default Admin User (CHANGE THESE IN PRODUCTION!)
DEFAULT_ADMIN_EMAIL=admin@yourcompany.com
DEFAULT_ADMIN_PASSWORD=your-secure-password-here
DEFAULT_ADMIN_NAME=System Administrator

# GitHub Integration (for issue reporting)
GITHUB_TOKEN=your-github-personal-access-token
GITHUB_REPO=ButterflyEA/treatments_manager

# Server Configuration
# Development: Automatically uses 127.0.0.1 (localhost only)
# Production: Automatically uses 0.0.0.0 (external access)
# Override with these variables if needed:
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8080

# Environment Detection (optional)
# ENVIRONMENT=production  # Forces production mode (0.0.0.0 binding)
# ENVIRONMENT=development # Forces development mode (127.0.0.1 binding)

# Logging Configuration (controls auto-detection)
# RUST_LOG=info     # Production mode (0.0.0.0)
# RUST_LOG=debug    # Development mode (127.0.0.1)
